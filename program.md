Your goal is to make the evaluation pass by fixing the greeting returned by `agent.py`.

Task rules:
- Only change the code needed to make the evaluation pass.
- Keep the implementation simple.
- Run `./eval/eval.sh` after each change.
- When the score reaches 1.0, commit and push your branch, then submit the run to Hive.

Expected outcome:
- `get_message()` must return exactly `Hello from Hive!`
